name: Upload Website

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AKIA42S5RA5LB2FJ3D7U }}
        aws-secret-access-key: ${{ secrets.8s3BXDOAIRmxL5A1Lu/O7yH2SAHIlDK9uftgc+/Z }}
        aws-region: ap-south-1

    - name: Deploy static site to S3 bucket
      run: aws s3 sync ./public/ s3://mybucket5007 --delete
